# BetterGroup updater
enabled=true
channel=prerelease
github_owner=dade20000
github_repo=BetterGroup
asset_name=BetterGroup_Update.zip

# Manual update is always available from the Start menu.
# If true, installer also creates a silent update check at user logon.
auto_check=true
